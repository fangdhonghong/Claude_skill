# 项目：<项目名>

> 这个文件是项目的"自描述"。3 个月后你回来看，5 分钟读完知道"这是什么项目、做到哪步、为什么这么决策"。
> AI 每次任务前都会读这个文件来重建上下文。

---

## 目标（一句话）

<例：通过 USART3 接收命令（"LED ON"/"LED OFF"），控制核心板上的 8 个 LED，作为后续项目的串口监控终端基础。>

---

## 工程根

- **project_root**：`<C:/Users/xxx/projects/my_stm32_app>`（AI 工作目录）
- **code 目录**：`<code>`（CubeMX 工程目录名；如果用户起的是别的名字如 `cs/` `myapp/`，记这里）

> skill 文档统一用 `code/` 这个名字指代 CubeMX 工程目录。如果实际目录名不同，AI 把所有 `code/` 路径理解为这里的"code 目录"字段对应的实际路径。

---

## 硬件

详见 [HARDWARE.md](./HARDWARE.md)。

简表：

- **MCU**：<STM32H775BG，WeAct Studio 核心板>
- **关键外设**：USART3（命令通道）、PC0-PC7（LED 输出）
- **外设芯片**：<如有>

---

## 婴儿步路线图

> 婴儿步模式：把大任务拆成多个可独立验证的小步。前几步通常是脚手架（验证硬件），最终要删；最后几步是正式功能。
> `[x]` = 已完成，`[ ]` = 未完成，`[~]` = 进行中，`[-]` = 已弃用

- [ ] **步骤 1**：流水灯验证 LED 硬件（脚手架，sandbox/） — <预期日期>
- [ ] **步骤 2**：串口回显验证 UART 通信（脚手架，sandbox/） — <预期日期>
- [ ] **步骤 3**：实现串口命令控制 LED（正式功能，app/） — <预期日期>
- [ ] **步骤 4**：实现自定义协议帧解析 + CRC16（正式功能，components/protocol/） — <预期日期>

---

## 关键决策摘要

> 只记**影响选型**的决策 + 原因。技术细节进 `tasks/` 对应任务日志。

- **YYYY-MM-DD**：选 USART3 + PD8/PD9 —— 原理图上 PA9/PA10 被 ST-LINK 占用，不能复用
- **YYYY-MM-DD**：LED 用 PC0-PC7 —— WeAct H7 核心板标配，无需飞线
- **YYYY-MM-DD**：MPU6050 AD0 拉低（地址 0x68）—— 板子默认设计
- **YYYY-MM-DD**：加速度量程 ±8g —— 项目用于无人机姿态，需要兼顾冲击容忍

---

## 历史任务索引（详见 `tasks/`）

> 按时间倒序。`[脚手架]` 任务可在验证后归档或删除；`[正式]` 任务长期保留。

- **YYYY-MM-DD** `<task-name>` `[脚手架/正式]` `<状态>` — <一句话摘要>
  - 例：`2026-07-16 led-test [脚手架] ✅ 验证通过 — 8 个 LED 循环亮灭`
- **YYYY-MM-DD** `<task-name>` ...

---

## 项目状态

- **当前进度**：<例：婴儿步 2/3，准备开始步骤 3>
- **下一步**：<例：实现串口命令解析，把 sandbox/uart_echo.c 升级为 app/app_cmd_handler.c>
- **暂停原因**（如适用）：<例：等 MPU6050 到货>
